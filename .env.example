# Required: Your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Override the model (default: claude-sonnet-4-5-20250929)
# MODEL=claude-opus-4-6

# Optional: Max conversation turns per ticket (default: 50)
# MAX_TURNS=50

# Optional: Max spend per ticket in USD (default: 5.00)
# MAX_BUDGET_USD=5.00

# Optional: Batch concurrency — how many tickets to process at once (default: 1)
# CONCURRENCY=2

# ── Auth ──────────────────────────────────────────────────────────
# Set this to protect the /enhance endpoint. The Chrome extension sends
# this as a Bearer token. Leave empty for local dev (no auth).
# AUTH_TOKEN=your-secret-token-here

# ── MCP Mode ──────────────────────────────────────────────────────
# "local"  (default) — loads MCP servers from ~/.claude.json
# "remote" — reads MCP server URLs from env vars below
# MCP_MODE=remote

# ── MCP Server URLs (only needed when MCP_MODE=remote) ───────────
# These are the HTTP MCP endpoint URLs for each service.
# Get these from your MCP provider dashboards.
# MCP_LINEAR_URL=https://mcp.linear.app/mcp
# MCP_NOTION_URL=https://mcp.notion.com/mcp
# MCP_SENTRY_URL=https://mcp.sentry.dev/mcp
# MCP_GRANOLA_URL=https://mcp.granola.ai/mcp
# MCP_SLACK_URL=https://mcp.slack.com/mcp
# MCP_FIGMA_URL=http://127.0.0.1:3845/mcp
# MCP_METABASE_URL=https://your-metabase-mcp-url/mcp
